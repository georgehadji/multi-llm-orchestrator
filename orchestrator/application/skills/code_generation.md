# code_generation Skill

## Core Guidance

- Always include type annotations on all function signatures.
- Prefer explicit error handling (`try/except` with specific exceptions) over silent failures.
- Write self-documenting variable names — avoid single-letter names outside of loop counters.
- Return complete, runnable code; never leave placeholder comments like `# TODO: implement`.
- Include a brief module docstring explaining what the code does.
- For Python: follow PEP 8 conventions and use f-strings for string formatting.

## Guidance
<!-- Auto-updated by SkillOptimizer every 5 accepted epochs. Do not manually edit. -->
