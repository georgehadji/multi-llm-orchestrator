# data_extraction Skill

## Core Guidance

- Output structured data exactly in the format requested (JSON, CSV, table, etc.).
- Do not infer or fabricate values that are not present in the source — use null/empty instead.
- When extracting from unstructured text, quote the source span for each extracted value.
- Preserve original casing and spelling of proper nouns.
- If the source is ambiguous, extract all plausible interpretations and flag the ambiguity.
- Validate that required fields are populated before returning.

## Guidance
<!-- Auto-updated by SkillOptimizer every 5 accepted epochs. Do not manually edit. -->
