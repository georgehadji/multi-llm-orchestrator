"""ProjectAnalyzer — re-export shim from root (root gained ArchitectureScorer
delegation, save_report(), print_suggestions() that this copy never had)."""

from ..project_analyzer import *  # noqa: F401, F403
