"""Python library scaffold template.

Author: Georgios-Chrysovalantis Chatzivantsidis
"""

from __future__ import annotations

FILES: dict[str, str] = {
    "src/__init__.py": '"""My library."""\n__version__ = "0.1.0"\n',
    "src/core.py": '"""Core library functionality."""\n\n\ndef hello() -> str:\n    return "Hello from library!"\n',
    "tests/__init__.py": "",
    "tests/test_core.py": (
        "from src.core import hello\n\n\n"
        "def test_hello():\n"
        '    assert hello() == "Hello from library!"\n'
    ),
    "pyproject.toml": (
        "[project]\n"
        'name = "my-library"\n'
        'version = "0.1.0"\n'
        'requires-python = ">=3.11"\n'
        "dependencies = []\n\n"
        "[build-system]\n"
        'requires = ["setuptools>=68"]\n'
        'build-backend = "setuptools.build_meta"\n'
    ),
    ".gitignore": "__pycache__/\n*.py[cod]\n.env\nvenv/\n.venv/\ndist/\nbuild/\n*.egg-info/\n",
    "README.md": r"""# 📦 Python Library

**Project Type:** Python Library/Package
**Platform:** Windows, macOS, Linux

---

## ⚡ Quick Start (Windows)

### Prerequisites
- [Python 3.11+](https://python.org/downloads/)

### 1. Create Virtual Environment
```powershell
python -m venv venv
venv\Scripts\activate
```

### 2. Install in Development Mode
```powershell
pip install -e .
```

### 3. Use the Library
```powershell
python
>>> from src.core import hello
>>> hello()
'Hello from library!'
```

---

## 📦 Run Tests
```powershell
venv\Scripts\activate
pytest
```

---

## 📁 Project Structure
```
├── src/
│   ├── __init__.py
│   └── core.py        # Library code
├── tests/
│   └── test_core.py   # Unit tests
└── pyproject.toml     # Package config
```

---

**Generated by Multi-LLM Orchestrator** — *This IS a Python library.*
""",
    ".env.example": "# Environment variables\n",
}
