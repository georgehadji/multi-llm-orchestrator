"""Operations package.

Submodules are importable directly, e.g.:
    from orchestrator.operations.diagnostics import SystemDiagnostics

Wildcard imports are intentionally absent — several submodules have
optional or unresolved transitive dependencies that would cause
ImportError when the package is imported.
"""
