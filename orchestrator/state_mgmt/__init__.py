"""State Management package.

Submodules are importable directly, e.g.:
    from orchestrator.state_mgmt.telemetry_store import TelemetryStore

Wildcard imports are intentionally absent — several submodules have
optional or unresolved transitive dependencies.
"""
